// ===================================
// Поиск в шапке — форма отправляется на ?s=запрос (WordPress)
// ===================================

/**
 * Инициализация поиска: формы с action и method="get" работают нативно.
 * Дополнительная логика не требуется.
 */
export function initSearch() {
    // Формы поиска (header__search, header__mobile-search) отправляются на home_url/?s=...
    // WordPress обрабатывает через search.php
}
